/**
 * Default timeout for waiting for a signal (10 minutes).
 */
export const DEFAULT_TIMEOUT_MS = 600000;

/**
 * Default polling interval (5 seconds).
 */
export const DEFAULT_POLL_INTERVAL_MS = 5000;
